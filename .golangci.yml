run:
  timeout: 5m

version: 2

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - revive
    - gocyclo
    - nestif
    - misspell
    - unparam
    - nilerr
    - prealloc
    - goconst
    - gocritic

issues:
  exclude-use-default: false
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
    - path: examples/
      linters:
        - errcheck
  max-issues-per-linter: 0
  max-same-issues: 0
